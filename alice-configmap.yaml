#1. create a user name Alice 

apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
data:
  # add an additional local user with apiKey and login capabilities
  #   apiKey - allows generating API keys
  #   login - allows to login using UI
  accounts.alice: apiKey, login
  # disables user. User is enabled by default
  accounts.alice.enabled: "true"

#2.update his password using Cli

a. login using cli
'argocd login argocd.minikube.local'
admin(user & password)

b- update password of user'alice'
'argocd account update-password --account alice --new-password <alicePassword>'
$ argocd account update-password --account alice --new-password alice@2024
argocd login argocd.minikube.local --username alice --password alice@2024


